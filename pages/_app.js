import '../styles/globals.css'; // Adjust the path if your CSS file is named or located differently

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
